services:
  server:
    container_name: fcgiserver
    build: ./server/
    image: anotherfcgi:mytag2

  nginx:
    image: nginx:latest
    ports:
      - "80:8080"
    volumes:
      - "./nginx/nginx.conf:/etc/nginx/nginx.conf"